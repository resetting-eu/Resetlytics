import { SignInForm } from '@/components/ui/auth/signinform';
import { Typography } from '@mui/material';

export default function Page() {
  // return (
  //   <Typography variant="h6" color="RoyalBlue" sx={{ paddingLeft: 4, mb: 2 }}>
  //     For the purpose of demonstrating the plaform, sign in is not required yet.
  //   </Typography>
  // );
  return (
    <SignInForm />
  )
}
