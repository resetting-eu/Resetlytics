import { Inter } from 'next/font/google';

const titleFont = Inter({
  weight: ['400', '700'],
  style: ['normal',],
  subsets: ['latin'],
  display: 'swap',
});

const textFont = Inter({
    weight: ['400', '700'],
    style: ['normal',],
    subsets: ['latin'],
    display: 'swap',
  });
